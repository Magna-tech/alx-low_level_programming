#include <stdio.h>

/**
 * main - entry and exit point
 * Return: the program returns 0 since it is of type int
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
