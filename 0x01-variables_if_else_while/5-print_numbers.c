#include <stdio.h>

/**
 * main - entry and exit of program
 * Description: numbers less than ten
 * Return:0
 */
int main(void)
{
	int a = 0;

	while (a <= 9)
	{
		printf("%d",a);
		a++;
	}
	printf("\n");

	return (0);
}
